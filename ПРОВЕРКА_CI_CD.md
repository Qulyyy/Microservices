# Проверка результатов CI/CD

После успешного выполнения конвейера нужно проверить, что всё работает правильно.

## ✅ 1. Проверка GitHub Actions

### Где проверить:
1. Откройте ваш репозиторий на GitHub
2. Перейдите в вкладку **Actions**
3. Найдите последний запуск конвейера
4. Убедитесь, что все jobs выполнены успешно:
   - ✅ **Matrix: Run Tests** - все тесты прошли
   - ✅ **Build and Push to DockerHub** - образ отправлен
   - ✅ **Build and Deploy to Yandex Cloud** - развёртывание выполнено
   - ✅ **Build Other Services** - сборка завершена

## ✅ 2. Проверка DockerHub (Задание 1)

### Где проверить:
1. Откройте https://hub.docker.com
2. Войдите в свой аккаунт
3. Перейдите в **Repositories**
4. Найдите репозиторий `auth-service` (или `ваш_логин/auth-service`)

### Что проверить:
- ✅ Образ существует в репозитории
- ✅ Есть тег `latest`
- ✅ Есть теги с SHA коммита (например, `main-abc1234`)
- ✅ Время последнего обновления соответствует последнему push

### Пример URL:
```
https://hub.docker.com/r/ваш_логин/auth-service
```

### Проверка через командную строку:
```bash
# Проверка наличия образа
docker pull ваш_логин/auth-service:latest

# Просмотр информации об образе
docker inspect ваш_логин/auth-service:latest
```

## ✅ 3. Проверка Яндекс.Облако (Задание 2)

### 3.1. Проверка Container Registry

**Где проверить:**
1. Откройте https://console.cloud.yandex.ru
2. Выберите ваш каталог
3. Перейдите в **Container Registry**
4. Откройте ваш реестр (например, `ikbo04`)

**Что проверить:**
- ✅ Репозиторий `user-service` существует
- ✅ Есть образ с тегом `latest`
- ✅ Время последнего обновления соответствует последнему push
- ✅ Размер образа указан

**Пример:**
```
Container Registry → ikbo04 → user-service → latest
```

### 3.2. Проверка Serverless Containers

**Где проверить:**
1. В Яндекс.Облаке перейдите в **Serverless Containers**
2. Найдите ваш контейнер (например, `laptev-user-service`)

**Что проверить:**
- ✅ Контейнер существует
- ✅ Есть активная ревизия (revision)
- ✅ Указан правильный образ: `cr.yandex/ваш_реестр/user-service:latest`
- ✅ Статус: **Active** или **Running**
- ✅ Указаны ресурсы: CPU, Memory
- ✅ Есть URL для доступа (если включён публичный доступ)

**Проверка ревизий:**
1. Откройте контейнер
2. Перейдите на вкладку **Ревизии**
3. Убедитесь, что последняя ревизия:
   - ✅ Создана недавно (после последнего push)
   - ✅ Статус: **Active**
   - ✅ Использует правильный образ

### 3.3. Проверка работы сервиса

**Если включён публичный доступ:**

1. Скопируйте URL контейнера (находится на странице контейнера)
2. Проверьте доступность:
   ```bash
   curl https://ваш-url-контейнера/health
   ```

3. Или откройте в браузере:
   ```
   https://ваш-url-контейнера/health
   ```

**Ожидаемый ответ:**
```json
{
  "status": "healthy",
  "service": "user_service"
}
```

**Проверка через YC CLI:**
```bash
# Список контейнеров
yc serverless container list

# Информация о контейнере
yc serverless container get --name ваш-контейнер

# Список ревизий
yc serverless container revision list --container-name ваш-контейнер

# Информация о последней ревизии
yc serverless container revision get --container-name ваш-контейнер --id последний-id
```

## ✅ 4. Проверка через логи

### GitHub Actions логи:
1. В GitHub Actions откройте последний успешный запуск
2. Проверьте логи каждого job:
   - **Build and Push to DockerHub**: должны быть строки "Pushed" или "Published"
   - **Build and Deploy to Yandex Cloud**: должны быть строки "revision created" или "deployed"

### Яндекс.Облако логи:
1. Откройте ваш Serverless Container
2. Перейдите на вкладку **Логи**
3. Проверьте логи последней ревизии
4. Убедитесь, что нет ошибок запуска

## ✅ 5. Тестирование развёрнутого сервиса

### Проверка health endpoint:
```bash
# Если есть публичный URL
curl https://ваш-url/health

# Или через внутренний доступ
curl http://localhost:5002/health
```

### Проверка API endpoints:
```bash
# Пример для user_service
curl https://ваш-url/users
curl https://ваш-url/leaderboard
```

## ✅ 6. Проверка автоматического развёртывания

### Тест автоматического обновления:
1. Внесите небольшое изменение в код `user_service`
2. Сделайте commit и push в ветку `main` или `master`
3. Дождитесь выполнения конвейера
4. Проверьте, что:
   - ✅ Новая ревизия создана в Serverless Containers
   - ✅ Образ обновлён в Container Registry
   - ✅ Сервис работает с новыми изменениями

## 📊 Сводная таблица проверки

| Компонент | Где проверить | Что проверить | Статус |
|-----------|---------------|---------------|--------|
| **GitHub Actions** | GitHub → Actions | Все jobs успешны | ✅ |
| **DockerHub** | hub.docker.com | Образ `auth-service` загружен | ✅ |
| **Yandex Container Registry** | console.cloud.yandex.ru | Образ `user-service` загружен | ✅ |
| **Serverless Container** | console.cloud.yandex.ru | Контейнер развёрнут и активен | ✅ |
| **Сервис работает** | URL контейнера | Health endpoint отвечает | ✅ |

## 🔍 Решение проблем

### Образ не появился в DockerHub:
- Проверьте секреты `DOCKERHUB_USERNAME` и `DOCKERHUB_TOKEN`
- Убедитесь, что репозиторий существует на DockerHub
- Проверьте логи в GitHub Actions

### Образ не появился в Yandex Container Registry:
- Проверьте секрет `YC_REGISTRY_ID`
- Убедитесь, что реестр существует
- Проверьте права сервисного аккаунта

### Контейнер не развёрнут:
- Проверьте все секреты для Яндекс.Облако
- Убедитесь, что сервисный аккаунт имеет роль `iam.serviceAccounts.user`
- Проверьте логи в GitHub Actions и Яндекс.Облаке

### Сервис не отвечает:
- Проверьте, включён ли публичный доступ
- Проверьте логи контейнера в Яндекс.Облаке
- Убедитесь, что образ собран правильно

## ✅ Итоговая проверка

После выполнения всех проверок убедитесь, что:

1. ✅ **CI/CD конвейер работает автоматически** при каждом push
2. ✅ **Образы загружаются** в DockerHub и Yandex Container Registry
3. ✅ **Сервисы развёртываются** автоматически
4. ✅ **Всё работает** и доступно

Поздравляем! 🎉 Ваш CI/CD конвейер настроен и работает!

