# Веб-интерфейс User Service

## Описание

Создан веб-интерфейс для визуализации и тестирования микросервиса `user_service`.

## Функциональность

### ✅ Проверка здоровья сервиса
- Автоматическая проверка статуса сервиса
- Индикатор онлайн/офлайн

### 👤 Управление профилями
- **Просмотр профиля**: получение информации о пользователе по ID
- **Обновление профиля**: изменение рейтинга и количества решённых задач

### 🏆 Табло лидеров
- Просмотр рейтинга пользователей
- Отображение топ пользователей с рейтингом

### 👥 Управление командами
- **Создание команды**: создание новой команды
- **Список команд**: просмотр всех команд
- **Присоединение к команде**: добавление пользователя в команду

## Запуск

### Локально

1. Запустите сервис:
   ```bash
   docker-compose up user_service
   ```

2. Откройте в браузере:
   ```
   http://localhost:5002
   ```

### В Serverless Containers

1. После развёртывания откройте URL контейнера:
   ```
   https://ваш-url-контейнера
   ```

## Использование

### Получение токена авторизации

Для работы с защищёнными endpoints нужен JWT токен:

1. Зарегистрируйтесь через `auth_service`:
   ```bash
   curl -X POST http://localhost:5001/register \
     -H "Content-Type: application/json" \
     -d '{"username": "testuser", "password": "password123", "email": "test@example.com"}'
   ```

2. Войдите и получите токен:
   ```bash
   curl -X POST http://localhost:5001/login \
     -H "Content-Type: application/json" \
     -d '{"username": "testuser", "password": "password123"}'
   ```

3. Используйте полученный токен в веб-интерфейсе

## Особенности

- **Адаптивный дизайн**: работает на всех устройствах
- **Автоматическое определение API URL**: работает локально и в Serverless Containers
- **Визуальная обратная связь**: цветовая индикация статусов и ошибок
- **Удобный интерфейс**: вкладки для организации функций

## Технологии

- HTML5
- CSS3 (Flexbox, Grid)
- Vanilla JavaScript (без фреймворков)
- Flask для серверной части

